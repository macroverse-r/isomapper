% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-country.r
\name{.get_ctry_name}
\alias{.get_ctry_name}
\title{Convert ISO code to country name}
\usage{
.get_ctry_name(iso_code, verbose = TRUE)
}
\arguments{
\item{iso_code}{Character vector of ISO3 country codes}

\item{verbose}{Logical, whether to print information about matches found in special or historical
code lists (default: TRUE)}
}
\value{
Character vector of country names. Returns NA for ISO codes not found in any lookup table.
When verbose is TRUE, prints informative messages for:
\itemize{
\item Matches found in special/regional codes list
\item Matches found in historical codes list
\item ISO codes not found in any list
}
}
\description{
Internal helper function that converts ISO3 codes to their corresponding country names
using a hierarchical lookup across multiple tables.
}
\details{
The function performs lookups in the following order:
\enumerate{
\item Current ISO codes (list_iso2country)
\item Special and regional codes (list_iso2country_others)
\item Historical codes (list_iso2country_old)
}
}
\examples{
\dontrun{
# Current ISO code
.get_ctry_name("FRA")  # Returns "France"

# Special regional code
.get_ctry_name("EMU")  # Returns "Euro Area" with note if verbose=TRUE

# Historical code
.get_ctry_name("CSK")  # Returns "Czechoslovakia" with note if verbose=TRUE

# Multiple codes
.get_ctry_name(c("USA", "EMU", "CSK"))  
# Returns c("United States", "Euro Area", "Czechoslovakia")
# with appropriate notes for EMU and CSK if verbose=TRUE
}
}
\keyword{internal}
